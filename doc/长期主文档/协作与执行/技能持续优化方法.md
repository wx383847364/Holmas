# 技能持续优化方法

## Summary

当前 `doc` 目录已存在，适合新增一份专门记录“如何持续优化 skill”的文档。  
建议保存为：

- `doc/skill 持续优化方法.md`

文档内容直接整理我刚才那次回答，定位为“项目过程中的 skill 迭代方法论”，供你后续反复参考和增补。

## Key Changes

建议新增文档结构：

- 标题：`Skill 持续优化方法`
- 开头摘要：
  - skill 不是一次写完的
  - 应该随着返工、review、subagent 协作、脚本检查、新模块接入不断迭代

正文按 5 个来源组织：
- 从返工里提炼规则
- 从 code review 里提炼规则
- 从 subagent 协作里提炼规则
- 从脚本检查里提炼规则
- 从新模块接入里提炼规则

再补 3 段固定内容：
- 推荐优化节奏：
  - 每完成一个模块，小步更新一次 skill
- 每次优先改哪里：
  - `SKILL.md`
  - `references/`
  - `scripts/`
- 项目专属优先沉淀项：
  - `MinesweeperTerrainData` 只做模板
  - Shared DTO 只能边界 agent 改
  - 奖励公式不能写进 UI
  - 地图生成依赖任务栏猫种池
  - 广告/离线归时间与持久化模块
  - YooAssets key 和资源分类要统一
  - 高风险目录必须独占写入

结尾保留一句总原则：
- 把“这次靠经验兜住的问题”，逐步变成“下次谁来做都不会轻易犯的规则”

## Public APIs / Interfaces

这份文档是方法文档，不新增代码接口。  
它只会作为后续 skill 演进参考，不修改现有 `App.Shared` / `App.HotUpdate` / `MinesweeperTerrainData` 结构。

建议与这些已有文档并列放置：
- `/Users/bruce/work/Holmas/doc/长期主文档/架构与边界/热更新边界规范_v1.md`
- `/Users/bruce/work/Holmas/doc/长期主文档/协作与执行/子代理与技能配对表.md`

## Test Plan

保存后应检查：
- 文件位于项目根目录 `doc/`
- 标题与内容能一眼区分“skill 草案”和“skill 优化方法”
- 内容不重复抄前两份文档，而是专注于“如何迭代 skill”
- 后续做完模块时，可以直接往这份文档追加项目经验

## Assumptions

- 当前仍处于 Plan Mode，所以这轮只规划保存方案，不直接写文件。
- 文件名默认采用中文，便于你和策划文档放在一起管理。
- 若切回可执行模式，下一步可直接把这份内容落到 `doc/skill 持续优化方法.md`。
