interface:
  display_name: "Unity HotUpdate Boundary"
  short_description: "Enforce Unity HotUpdate and YooAssets boundaries"
  default_prompt: "Use $unity-hotupdate-boundary to implement or review this project while keeping AOT/Shared/HotUpdate boundaries, runtime state ownership, YooAssets loading, and WeChat MiniGame CDN rules consistent."

policy:
  allow_implicit_invocation: true
