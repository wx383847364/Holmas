# Golden Case Expectations

At minimum keep one repeatable case for:

- a sample approved spec
- a sample generated manifest
- a Holmas adapter mapping example
- a validator baseline that can be rerun without manual editing

Use these cases to detect:

- random naming drift
- non-local prefab changes
- missing slot reporting regressions
- adapter mapping regressions
