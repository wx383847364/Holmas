# UI 自动生成系统隔离化孵化方案 v2

## Summary

按你刚定下来的边界，这套系统先 **不单开顶层目录**，但要做到“在现有仓库里强隔离”：

- 文档仍放 `doc/长期主文档`
- 代码仍留在 Holmas 现有工程目录
- 但文档、方案、代码、脚本、示例、测试都必须有 **自己的独立分区**
- 不再混进现有 `方案与数据`、`协作与执行`、Holmas 业务 UI 文档里
- 后续拆项目时，按“专区整体迁移”的方式抽走

这意味着第一版要把“隔离”做在 **目录、命名空间、asmdef、文档索引、脚本入口、测试入口** 这 6 个层面，而不是只靠文件名区分。

## Isolated Layout

### 1. 文档隔离

在 `doc/长期主文档` 下新建一个完全独立专区，建议固定为：

- `doc/长期主文档/UI自动生成系统/00_总览.md`
- `doc/长期主文档/UI自动生成系统/01_产品目标与边界.md`
- `doc/长期主文档/UI自动生成系统/02_架构设计.md`
- `doc/长期主文档/UI自动生成系统/03_Agent编组方案.md`
- `doc/长期主文档/UI自动生成系统/04_输入规格_DesignPacket.md`
- `doc/长期主文档/UI自动生成系统/05_中间规格_UiPrefabSpec.md`
- `doc/长期主文档/UI自动生成系统/06_输出规格_PrefabBindingManifest.md`
- `doc/长期主文档/UI自动生成系统/07_生成流程与审阅流程.md`
- `doc/长期主文档/UI自动生成系统/08_校验与回归策略.md`
- `doc/长期主文档/UI自动生成系统/09_Holmas接入约束.md`

规则：
- 这套系统的长期方案、接口、规范、agent 规划，只能写进这个专区
- 现有 Holmas 文档只保留“引用链接”和“接入说明”，不再承载系统本体方案
- `主文档索引.md` 只新增一个入口链接，不展开细节

### 2. 代码隔离

虽然代码继续留在 Holmas 工程里，但必须独立成一块系统区，建议固定为：

- `Assets/Tools/UiPrefabGenerator/Runtime`
- `Assets/Tools/UiPrefabGenerator/Editor`
- `Assets/Tools/UiPrefabGenerator/Tests`
- `Assets/Tools/UiPrefabGenerator/Samples~/Holmas`
- `Assets/Tools/UiPrefabGenerator/Documentation~`

即使不做顶层独立仓库，也先按“未来包结构”组织。

规则：
- 这套系统不写进 `Assets/HotUpdateContent/Script/App.HotUpdate/Holmas/UI`
- 不写进 `Assets/Scripts/App.Shared`
- 不写进现有 Holmas gameplay 目录
- Holmas 只通过 adapter 或接入层调用它

### 3. 程序集隔离

第一版就拆独立 asmdef：

- `UiPrefabGenerator.Core`
- `UiPrefabGenerator.Editor`
- `UiPrefabGenerator.Tests`
- `UiPrefabGenerator.HolmasAdapter`

规则：
- 生成器主逻辑不依赖 Holmas gameplay asmdef
- Holmas 适配只放在 `HolmasAdapter`
- 未来抽离时，只需要去掉 adapter 并替换项目 profile

## Agent Plan

### 1. Foundation / Schema Agent
职责：
- 冻结专区目录
- 冻结 asmdef 分层
- 定义 `DesignPacket` / `UiPrefabSpec` / `PrefabBindingManifest`
- 定义 Holmas 适配边界

写入范围：
- `UI自动生成系统` 文档专区
- `Assets/Tools/UiPrefabGenerator/*` 的 schema 和接口层

### 2. Design Intake / Spec Agent
职责：
- 负责把设计图 + 标注包解释成结构化 spec
- 输出 spec 草稿与人工审阅稿
- 不直接生成 Unity 资产

写入范围：
- spec 解释器
- 输入校验器
- spec 文档样例

### 3. Unity UGUI Generator Agent
职责：
- 根据批准后的 spec 生成 prefab 草稿
- 生成节点层级、组件、布局、资源槽位、绑定清单
- 不直接写业务 Presenter

写入范围：
- `UiPrefabGenerator.Editor`
- 草稿 prefab 输出目录
- manifest 生成逻辑

### 4. Validation / Regression Agent
职责：
- spec 校验
- 生成结果校验
- deterministic 回归
- golden case 管理

写入范围：
- tests
- validators
- regression fixtures

### 5. Review / Acceptance Agent
职责：
- 审 schema
- 审 spec
- 审 prefab 草稿
- 审回归覆盖
- 给阶段性结论

## Workflow

冻结为这条固定流水线：

1. 设计侧提交 `DesignPacket`
2. `Spec Agent` 生成 `UiPrefabSpec`
3. 人工审 spec
4. `Generator Agent` 生成 prefab 草稿 + `PrefabBindingManifest`
5. `Validation Agent` 跑校验与回归
6. `Review Agent` 给结论
7. Holmas UI 实现方再做业务接线

第一版明确不允许：
- 从图片直接跳到正式 prefab
- 在 Holmas 业务目录里直接堆生成器逻辑
- spec、生成、校验写到同一批文件里
- 文档散落到现有 Holmas 方案文档中

## Important Interfaces

第一版冻结这些文件级契约，不允许后续随意混写到旧文档或旧模块：

- `DesignPacket`
设计输入包，描述设计图、标注、交互规则、状态与资源位。

- `UiPrefabSpec`
唯一机器权威中间层；所有生成和校验都基于它。

- `PrefabBindingManifest`
描述 prefab 输出结果里的节点路径、组件、资源槽位、事件出口、人工补位项。

- `ProjectUiProfile`
定义项目级命名、目录、默认组件、资源规则；Holmas 只实现这一层。

## Test Plan

第一版必须覆盖：

- 文档专区是否完整独立，可被单独索引
- schema 是否不依赖 Holmas gameplay 代码
- 同一 spec 重复生成是否稳定
- spec 改局部时 prefab 是否只局部变化
- validator 是否能拦住非法节点、非法组件、缺资源位
- Holmas adapter 是否只做项目映射，不把业务逻辑灌进生成器
- 从专区文档 + 代码目录是否能独立理解系统，而不必翻旧 Holmas 文档

## Assumptions

- 你接受“文档留在 `doc/长期主文档`，但必须有独立专区”这个折中方案
- 你接受“代码留在现有工程，但必须有独立 `Assets/Tools/UiPrefabGenerator` 系统区”这个折中方案
- 第一版仍只做 Unity UGUI
- 第一版仍只做 prefab 草稿 + binding manifest
- 人工审阅点保留在 spec 阶段和 prefab 草稿阶段
- Holmas 是试点项目，不是系统本体边界

## 和上一版规划的区别

上一版重点是 **Agent 编组和流水线角色**。  
这一版新增并冻结了 **隔离策略**，重点从“谁来做”扩展到“东西放哪、怎么隔离、以后怎么搬”。

新增的核心变化是：

- 文档不再混进现有 `方案与数据` / `协作与执行`
- 系统代码不再混进 Holmas 的 `App.HotUpdate/Holmas/UI`
- 增加独立 asmdef 和 adapter 分层
- Holmas 从“实现主体”降级成“试点接入方”
- 未来拆项目时有明确的迁移边界

## 优势

### 1. 后续拆出成本更低
虽然现在没单开仓库，但目录、程序集、文档入口都已经独立，后面搬迁是“专区迁移”，不是“全仓搜索拆线”。

### 2. 不会继续污染旧文档
这套系统的长期方案、agent 规划、输入输出契约都有自己的专区，Holmas 旧文档只保留引用关系。

### 3. 不会把生成器做成 Holmas 私有逻辑
代码不进业务 UI 目录，生成器和 Holmas 适配层明确分开，后面换项目时不至于重写一遍。

### 4. 更适合长期迭代
你后面要反复优化的是输入 spec、生成质量、回归稳定性、审阅流程；这些现在都有独立文件区和模块区，便于持续演进。

### 5. 仍符合你当前现实约束
这版没有强行要求你现在就单开仓库，也没有要求把所有东西移出 Holmas；是在你刚刚选定的边界下，把隔离做到最强。
