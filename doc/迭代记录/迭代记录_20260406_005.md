# 迭代记录 2026-04-06 005

## 迭代标识

- 迭代编号：20260406-005
- 记录日期：2026-04-06
- 当前状态：进行中
- 承接上一轮：[迭代记录 2026-04-06 004](/Users/bruce/work/Holmas/doc/迭代记录/迭代记录_20260406_004.md)
- 建议起始 Agent：待补充

---

## 本轮主题

协作文档入口与调度流程优化

## 本轮目标

- [ ] 把新会话入口从口令驱动改成自动判断，并明确执行方案里的 subagent 决策与收尾格式

## 当前状态

- 当前阶段：进行中
- 整体判断：待补充

## 关键结论

- 待补充

## 分工状态

- Agent 1：Shared 与骨架继续保持冻结，本轮未改
- Agent 2：地图与棋盘纯逻辑作为被验证对象，本轮未改生产逻辑
- Agent 3：任务与长期进度纯逻辑作为被验证对象，本轮未改生产逻辑
- Agent 4：UI 与验证，暂未启动
- Agent 5：测试与质量保障，默认按需启动，本轮未启动
- Agent 6：挑刺与问题审查，默认在阶段里程碑完成后按需启动

## 工作日志

### 2026-04-06 23:34

- 按 7 收 5 方案合并协作与执行文档：模板类并入 skill 与 subagent 任务模板，编组类并入 subagent 与 skill 配对表，并保留跳转页兼容旧链接

### 2026-04-06 23:33

- 按 7 收 5 方案合并协作文档，收拢模板类与编组类并保留旧页跳转兼容

### 2026-04-06 23:18

- 统一线程级辅助 subagent 角色等文档的入口措辞，改成主线程自动判断 helper 是否启用

### 2026-04-06 23:13

- 优化新会话入口文档与 Agent 调度说明，改为默认自动判断并明确 subagent 方案与收尾格式

### 2026-04-06 23:13

- 创建本轮迭代记录

## 完成项

- 已启动 Agent 6 风格只读复审，结论为通过，无阻塞问题
- 同步更新 Codex新会话必读、Agent 启动与验收规范、线程级辅助 subagent 角色 等文档的 cross-link，统一到新的权威页
- 将 长期 subagent 编组方案 的长期版说明并入 subagent 与 skill 配对表，旧页改为跳转页
- 将 Agent 启动口令清单 的覆盖模板并入 skill 与 subagent 任务模板，旧页改为跳转页
- 按 Agent 6 口径完成兜底复审，当前未发现会误导新会话或误导脚本的阻塞问题
- 统一入口页、总规范页、helper 页和 UI 自动生成系统派工单中的主链接，避免继续把跳转页当成权威正文
- 将 长期 subagent 编组方案 的长期版说明并入 subagent 与 skill 配对表，并把旧页改成跳转页
- 将 Agent 启动口令清单 的覆盖模板并入 skill 与 subagent 任务模板，并把旧页改成跳转页
- 同步更新 Agent 启动口令清单 和 skill 与 subagent 任务模板，把 helper 模板降级为显式覆盖模板，并统一 execution planning 表述
- 补充 helper 启用条件、主线程直做优先原则，以及不需要 helper 时的回显要求
- 改写 线程级辅助 subagent 角色，明确 helper 属于默认入口后的自主判断，不依赖用户补口令
- 同步修正文档间引用关系，把 Agent 启动与验收规范、Agent 启动口令清单、skill 与 subagent 任务模板改成入口自动判断、口令仅作覆盖模板
- 补充执行前方案格式，要求明确写出主线程直做还是启真实 subagent、判断理由、里程碑和收尾方式
- 重写 Codex新会话必读，去掉默认入口口令，改成新会话读到即按长期规则执行

## 风险与阻塞

- 当前仓库同时存在用户自己的 Unity 工具与脚本改动；本轮未触碰这些实现文件

## 下一步

- 如果后续还要继续降低文档心智负担，可再为模板页和配对页补页内目录或速查索引
- 如需继续压缩文档心智负担，可再考虑给 skill 与 subagent 任务模板 和 subagent 与 skill 配对表 增加页内目录或速查索引
- 如需继续收紧，可再检查 UI 自动生成系统专区和其他专项文档是否仍残留旧入口措辞
- 如后续还要继续收紧流程，可再统一线程级辅助 subagent 角色等文档中的入口措辞

## 给下一轮的人

- 待补充
